os: windows
and app.name: Microsoft Excel
os: windows
and app.exe: EXCEL.EXE
--

change : key(ctrl-shift-a)

add row : key(ctrl-shift-b)

change back : key(ctrl-shift-c)

make day : key(ctrl-shift-e)
    
make half day : key(ctrl-shift-h)

calculate : key(ctrl-shift-k)

clear contents : 
    key(alt)
    key(h)
    key(e)
    key(c)